{% extends 'base.html.twig' %}

{% block title %}Hello MessageController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
{# display of firstname #}
<div class ="mb-5">
<a href="{{path('app_message')}}" >Back</a>
</div>
    <h1>Hello {{ app.user.firstname }} ! ✅</h1>
    <h3>You can see one message here</h3>

    <li>{{ message.id }} {{ message.content }}</li>


    {% if app.user == message.users %} {# If the user is the message's writter, the buttons "Modify" and "delete" are visible #}
    {# {{ dump(app.user) }} #}

    <a href="{{ path('message_edit', {id:message.id})}}" role="button" class="btn btn-warning mt-5 px-5">Modifier le message</a>
    {{ include('message/_delete_form.html.twig') }}
    {% endif %}

</div>
<nav>

{% endblock %}
